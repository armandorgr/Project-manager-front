import { PasswordValDirective } from './password-val.directive';

describe('PasswordValDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordValDirective();
    expect(directive).toBeTruthy();
  });
});
