import { ConfirmPasswordValDirective } from "./confirm-password-val.directive";

describe('ConfirmPasswordValDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmPasswordValDirective();
    expect(directive).toBeTruthy();
  });
});
